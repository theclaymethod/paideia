"""Starter — chunk {{CHUNK_ID}}: {{CHUNK_TITLE}}.  FILL IN.

Contracts only below — the learner derives the implementation from the lesson
(HOOK / RAMP). Give each function a precise docstring contract (inputs, output,
invariants, edge cases) but NO implementation: leave a
`raise NotImplementedError("TODO: ...")` hole so the two-sided gate fails
against this starter and passes against solutions/impl.py.

Implement the logic yourself; library one-liners that trivialize the point
are discouraged (and may not be installed).

Run the gate:  python3 <course>/scripts/check_gates.py --chunk {{CHUNK_ID}} --learner
"""
from __future__ import annotations


def solve(x):
    """<One-line summary of what this computes.>

    Contract:
      * <input>: describe the expected type/shape and any preconditions.
      * returns: describe the output type, units, and range.
      * invariants: e.g. non-negative; order-independent; f(0) == 0; ...
      * edge cases: describe degenerate inputs and the required behavior.
    """
    raise NotImplementedError("TODO: implement solve() from the lesson RAMP")
