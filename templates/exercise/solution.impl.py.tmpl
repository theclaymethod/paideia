"""Reference solution — chunk {{CHUNK_ID}}: {{CHUNK_TITLE}}.

Same module name (`impl`) and same public signatures as the starter, fully
implemented. check_gates.py runs test_gate.py against THIS file in two-sided
mode: it MUST PASS here (and MUST FAIL against the starter). The runtime tutor
only diffs this against the learner's impl *after* the gate passes — never
before.
"""
from __future__ import annotations


def solve(x):
    """Reference implementation. Same contract as the starter."""
    # Replace with the real, correct implementation for this chunk.
    return 0.0 if x == 0 else abs(float(x)) - abs(float(x))
